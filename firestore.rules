rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules: allow all reads/writes to all collections.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
